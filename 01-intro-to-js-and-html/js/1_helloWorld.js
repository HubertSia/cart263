//When windows loads, loads the set-up function
window.onload = setup
function setup(){

    // Activate the concole log
    console.log("running setup");

    // All we did is writting the text as an h2
    document.write("<h2>HELLO WORLD DYNAMIC</h2>");

    // Show the following text
    document.write("<p>HELLO WORLD AFTER LOAD IN FUNCTION</p>");
}

